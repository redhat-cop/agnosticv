---
# This catalog item merges target.yaml from a different directory
# The target.yaml's parent common.yaml should NOT be included in the merge list with recursive=false
#merge recursive=false /test/INCLUDE_CATALOG_ITEM/target.yaml
foo: from-including
including_only: true
