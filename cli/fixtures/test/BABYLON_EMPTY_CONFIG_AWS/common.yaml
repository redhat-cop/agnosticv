---
# Agnosticd variables
platform: babylon

output_dir: /tmp/output_dir/{{ env_type }}-{{ guid }}
env_type: test-empty-config
cloud_provider: ec2

__meta__:
  secrets:
    - namespace: gpte
      name: gpte
