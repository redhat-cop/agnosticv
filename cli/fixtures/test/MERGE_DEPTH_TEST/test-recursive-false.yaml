#merge recursive=false /test/MERGE_DEPTH_TEST/second.yaml

# With recursive=false, the merge list should be:
# [second.yaml, test-recursive-false.yaml]
# third.yaml is NOT included, so we define foo here
foo: from-first
only-in-first: true

config:
  level1:
    level2:
      foo: from-first
